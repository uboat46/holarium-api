services:
  ollama:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - MODEL=snowflake-arctic-embed2:568m
      - PORT=8080
    volumes:
      - ./models:/models
      - ollama_data:/root/.ollama
volumes:
  ollama_data: